# Done Checklist
- Run relevant tests at minimum (`python main.py --mode test` and targeted unit tests for touched code).
- Verify changed automation/scripts with syntax checks (e.g., `bash -n` for shell scripts).
- If docs changed, ensure commands and paths are accurate.
- For Obsidian tracking automation, verify updater command and (on host) `systemctl --user` timer status/logs.