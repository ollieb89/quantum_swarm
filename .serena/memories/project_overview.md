# Quantum Swarm Overview
- Purpose: Multi-agent trading system with OpenClaw integration plus local automation utilities.
- Main entrypoint: `main.py`.
- Primary areas: orchestrator (`src/orchestrator`), agents (`src/agents`), core wrappers (`src/core`), skills (`src/skills`), config (`config`), runtime/state (`data`), dashboard (`dashboard`).
- Additional project-tracking automation: Obsidian vault content under `quantum-swarm/Projects/quantum-swarm` and update script at `scripts/update_project_tracking.py`.