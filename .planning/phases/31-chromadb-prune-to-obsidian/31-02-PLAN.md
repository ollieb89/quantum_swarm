---
phase: 31-chromadb-prune-to-obsidian
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/main.py
  - tests/cli/test_prune_cli.py
autonomous: false
requirements:
  - OBS-03

must_haves:
  truths:
    - "Running 'python -m src.main prune --dry-run' shows a Rich table of what would be archived/deleted"
    - "Running 'python -m src.main prune' archives and deletes documents from ChromaDB"
    - "Running 'python -m src.main prune --no-archive' deletes without archiving"
    - "Prune subcommand is discoverable via 'python -m src.main --help'"
  artifacts:
    - path: "src/main.py"
      provides: "Prune subcommand registration and dispatch"
      contains: "register_prune_parser"
    - path: "tests/cli/test_prune_cli.py"
      provides: "CLI integration tests for prune subcommand"
      min_lines: 30
  key_links:
    - from: "src/main.py"
      to: "src/cli/prune.py"
      via: "register_prune_parser + handle_prune import and dispatch"
      pattern: "from src\\.cli\\.prune import"
---

<objective>
Wire the prune subcommand into main.py CLI dispatch and verify end-to-end with integration tests. Then human-verify the full prune flow.

Purpose: Connect the prune engine (Plan 01) to the CLI entry point so users can run `python -m src.main prune`.
Output: Updated main.py with prune dispatch, integration tests, human verification of full flow.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-chromadb-prune-to-obsidian/31-CONTEXT.md
@.planning/phases/31-chromadb-prune-to-obsidian/31-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/cli/prune.py (created in Plan 01):
```python
from argparse import Namespace, _SubParsersAction

def register_prune_parser(sub: _SubParsersAction) -> None:
    """Add 'prune' subcommand with --dry-run, --days N, --no-archive flags."""
    ...

def handle_prune(args: Namespace) -> int:
    """Top-level prune dispatch. Returns exit code (0=success, 1=error)."""
    ...
```

From src/main.py (current dispatch pattern):
```python
from src.cli.replay import handle_replay, register_replay_parser
# ...
register_replay_parser(sub)
# ...
if args.command == "replay":
    sys.exit(handle_replay(args))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire prune subcommand into main.py and add CLI integration tests</name>
  <files>src/main.py, tests/cli/test_prune_cli.py</files>
  <action>
1. Update `src/main.py`:
   - Add import after the replay import: `from src.cli.prune import handle_prune, register_prune_parser  # noqa: E402`
   - After `register_replay_parser(sub)`, add: `register_prune_parser(sub)`
   - In the dispatch section, before the `if args.command != "analyze"` check, add:
     ```python
     if args.command == "prune":
         sys.exit(handle_prune(args))
     ```

2. Create `tests/cli/test_prune_cli.py` with integration-level tests:
   - Test that `register_prune_parser` adds "prune" to subparsers and parses --dry-run, --days, --no-archive flags correctly
   - Test that handle_prune returns 0 on success with mocked MemoryService (empty collection = nothing to prune)
   - Test that handle_prune returns 1 when ChromaDB health_check fails (graceful degradation)
   - Mock MemoryService at the module level (patch `src.cli.prune.MemoryService`) to avoid needing real ChromaDB
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/cli/test_prune_cli.py tests/cli/test_prune.py -x -v</automated>
  </verify>
  <done>
    - main.py imports and registers prune subcommand
    - `python -m src.main prune --dry-run` executes without error (with mocked/real ChromaDB)
    - `python -m src.main --help` shows "prune" in available commands
    - CLI integration tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify prune CLI end-to-end</name>
  <files>src/main.py</files>
  <action>
Human verifies the complete prune CLI flow works correctly:
1. Run `python -m src.main --help` and confirm "prune" appears as a subcommand
2. Run `python -m src.main prune --dry-run` and verify it shows a table (or "nothing to prune" if ChromaDB is empty)
3. Run `python -m src.main prune --dry-run --days 1` to see if any recent documents would be archived
4. Check that `quantum-swarm/Archives/memory/` directory structure looks correct (if any documents were actually archived in a real run)
5. Confirm all tests pass: `.venv/bin/python3.12 -m pytest tests/cli/test_prune.py tests/cli/test_prune_cli.py -v`
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - Human confirms prune subcommand appears in --help
    - Human confirms dry-run output is sensible
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.12 -m pytest tests/cli/test_prune.py tests/cli/test_prune_cli.py -x -v` -- all prune tests green
- `.venv/bin/python3.12 -m pytest tests/ -x -q` -- full suite green
- `python -m src.main --help` shows prune subcommand
- `python -m src.main prune --dry-run` runs without error
</verification>

<success_criteria>
- Prune subcommand registered in main.py and reachable via CLI
- Full prune flow works end-to-end (dry-run and real mode)
- All tests green including full suite regression
- Human verified the CLI output
</success_criteria>

<output>
After completion, create `.planning/phases/31-chromadb-prune-to-obsidian/31-02-SUMMARY.md`
</output>
