---
phase: 09-structured-memory-registry
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - tests/test_structured_memory.py
  - src/core/memory_registry.py
autonomous: true
requirements:
  - MEM-04
  - MEM-05

must_haves:
  truths:
    - "Orchestrator _load_institutional_memory() returns only active rules from the JSON registry"
    - "RuleGenerator.persist_rules() writes rules as proposed to data/memory_registry.json"
    - "Rules written as proposed are not returned by get_active_rules() until promoted via update_status()"
    - "Full round-trip works: RuleGenerator writes proposed -> update_status promotes to active -> orchestrator injects it"
  artifacts:
    - path: "data/memory_registry.json"
      provides: "Persisted JSON registry created by test or real pipeline run"
      contains: "rules"
    - path: "tests/test_structured_memory.py"
      provides: "Integration tests covering full lifecycle round-trip"
      exports: []
  key_links:
    - from: "src/agents/rule_generator.py"
      to: "src/core/memory_registry.py"
      via: "registry.add_rule(rule)"
      pattern: "registry\\.add_rule"
    - from: "src/graph/orchestrator.py"
      to: "src/core/memory_registry.py"
      via: "registry.get_active_rules()"
      pattern: "get_active_rules"
---

<objective>
Verify the full MEM-04 + MEM-05 story with integration tests: RuleGenerator produces proposed JSON rules, update_status() promotes them to active, and orchestrator injection returns only active rules.

Purpose: The individual components (models, registry, rule_generator, orchestrator) were built separately. This plan proves they are correctly wired end-to-end without requiring a live LLM or database. It gives Phase 10 (Rule Validation Harness) a clear and tested promotion path to build against.

Output: Expanded integration tests in tests/test_structured_memory.py. No new production files — integration is already wired; this plan validates the wiring.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-structured-memory-registry/09-01-SUMMARY.md

<interfaces>
<!-- After plan 09-01, MemoryRegistry has: -->
```python
class MemoryRegistry:
    def add_rule(self, rule: MemoryRule): ...          # proposed by default
    def get_active_rules(self) -> List[MemoryRule]: ...
    def get_rule(self, rule_id: str) -> Optional[MemoryRule]: ...
    def update_status(self, rule_id: str, new_status: str): ...  # added in 09-01
    def save(self): ...                                           # atomic os.replace
```

<!-- RuleGenerator.persist_rules (src/agents/rule_generator.py) -->
```python
def persist_rules(self, rules: List[MemoryRule]):
    # For each rule: self.registry.add_rule(rule)  ← adds as proposed
    # Also appends to data/MEMORY.md for dual-write
```

<!-- RuleGenerator path attributes (src/agents/rule_generator.py) -->
# Verified: both registry and memory_md_path are plain public instance attributes
# set in __init__ with no property wrapping — direct assignment works:
#   rg.registry = MemoryRegistry(str(self.test_registry_file))   # confirmed settable
#   rg.memory_md_path = self.test_memory_md                       # confirmed settable
# Source: src/agents/rule_generator.py line 40-41

<!-- Orchestrator injection (src/graph/orchestrator.py) -->
```python
def _load_institutional_memory(self) -> str:
    registry = MemoryRegistry()
    rules = registry.get_active_rules()   # only status=="active"
    # formats and returns as string for prompt injection
    # fallback: reads data/MEMORY.md if registry empty
```

<!-- Mocking pattern for LLM-dependent agents (from STATE.md) -->
# Set agent._llm = MagicMock() to suppress GOOGLE_API_KEY requirement.
# For RuleGenerator: rg = RuleGenerator(); rg.llm = MagicMock()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration test — RuleGenerator persist_rules() stores proposed rules in registry</name>
  <files>tests/test_structured_memory.py</files>
  <behavior>
    - RuleGenerator.persist_rules([rule]) stores the rule in the JSON registry with status="proposed"
    - The stored rule is NOT returned by get_active_rules() (it is proposed, not active)
    - The stored rule IS returned when iterating registry.schema.rules
    - persist_rules() also appends to MEMORY.md (dual-write — verify file grows)
  </behavior>
  <action>
Add TestRuleGeneratorIntegration class to tests/test_structured_memory.py.

setUp:
  self.test_registry_file = Path("tests/temp_integration_registry.json")
  self.test_memory_md = Path("tests/temp_MEMORY.md")
  # Remove both if they exist from a prior run

tearDown:
  Remove both temp files if they exist.

test_persist_rules_stores_proposed:
  rg = RuleGenerator()
  rg.registry = MemoryRegistry(str(self.test_registry_file))
  rg.memory_md_path = self.test_memory_md
  # NOTE: memory_md_path is a plain public instance attribute (set in __init__ line 41
  # of src/agents/rule_generator.py). Direct assignment is correct — no mock needed.
  rule = MemoryRule(title="Integration Rule", type="risk_adjustment")
  rg.persist_rules([rule])
  # Assert in registry
  reloaded = MemoryRegistry(str(self.test_registry_file))
  self.assertEqual(len(reloaded.schema.rules), 1)
  self.assertEqual(reloaded.schema.rules[0].status, "proposed")
  # Assert NOT in active rules
  self.assertEqual(len(reloaded.get_active_rules()), 0)
  # Assert MEMORY.md was written
  self.assertTrue(self.test_memory_md.exists())

test_promote_rule_appears_in_active:
  rg = RuleGenerator()
  rg.registry = MemoryRegistry(str(self.test_registry_file))
  rg.memory_md_path = self.test_memory_md
  rule = MemoryRule(title="Promotable Rule", type="strategy_preference")
  rg.persist_rules([rule])
  # Now promote
  reloaded = MemoryRegistry(str(self.test_registry_file))
  stored_rule = reloaded.schema.rules[0]
  reloaded.update_status(stored_rule.id, "active")
  # Assert now active
  active = reloaded.get_active_rules()
  self.assertEqual(len(active), 1)
  self.assertEqual(active[0].title, "Promotable Rule")

Import RuleGenerator from src.agents.rule_generator at top of file.
Import MemoryRule from src.models.memory.
No LLM is needed — persist_rules() takes pre-built MemoryRule objects.
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_structured_memory.py::TestRuleGeneratorIntegration -v</automated>
  </verify>
  <done>2 integration tests pass. No live LLM or API key required. Dual-write to registry + MEMORY.md confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — Orchestrator _load_institutional_memory() injects only active rules</name>
  <files>tests/test_structured_memory.py</files>
  <behavior>
    - _load_institutional_memory() returns "No active institutional rules." when registry is empty
    - When registry has only proposed rules, returns "No active institutional rules." (or MEMORY.md fallback only)
    - When registry has an active rule, its title and id appear in the returned string
    - Active rule from registry and content from MEMORY.md are both included when both sources have data
  </behavior>
  <action>
Add TestOrchestratorMemoryInjection class to tests/test_structured_memory.py.

Import LangGraphOrchestrator from src.graph.orchestrator.
Use unittest.mock.patch to prevent YAML/LLM/MemoryService construction during tests.

setUp:
  self.test_registry_file = Path("tests/temp_orch_registry.json")
  if self.test_registry_file.exists(): self.test_registry_file.unlink()

tearDown:
  if self.test_registry_file.exists(): self.test_registry_file.unlink()

test_empty_registry_returns_fallback_message:
  orch = LangGraphOrchestrator.__new__(LangGraphOrchestrator)
  orch._yaml_config = {}
  # Patch MemoryRegistry inside _load_institutional_memory to use temp file
  with patch("src.graph.orchestrator.MemoryRegistry") as mock_reg_cls:
    mock_reg = MagicMock()
    mock_reg.get_active_rules.return_value = []
    mock_reg_cls.return_value = mock_reg
    # Also patch Path.exists to return False for MEMORY.md
    with patch("src.graph.orchestrator.Path") as mock_path_cls:
      mock_path_instance = MagicMock()
      mock_path_instance.exists.return_value = False
      mock_path_cls.return_value = mock_path_instance
      result = orch._load_institutional_memory()
  self.assertEqual(result, "No active institutional rules.")

test_active_rule_appears_in_injection:
  active_rule = MemoryRule(title="Buy on breakout", type="strategy_preference", status="active")
  with patch("src.graph.orchestrator.MemoryRegistry") as mock_reg_cls:
    mock_reg = MagicMock()
    mock_reg.get_active_rules.return_value = [active_rule]
    mock_reg_cls.return_value = mock_reg
    with patch("src.graph.orchestrator.Path") as mock_path_cls:
      mock_path_instance = MagicMock()
      mock_path_instance.exists.return_value = False
      mock_path_cls.return_value = mock_path_instance
      orch = LangGraphOrchestrator.__new__(LangGraphOrchestrator)
      orch._yaml_config = {}
      result = orch._load_institutional_memory()
  self.assertIn("Buy on breakout", result)
  self.assertIn(active_rule.id, result)

Import MagicMock and patch from unittest.mock.
Use LangGraphOrchestrator.__new__() to bypass __init__ (avoids config file reads, MemoryService, LangGraph compilation).
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_structured_memory.py::TestOrchestratorMemoryInjection -v</automated>
  </verify>
  <done>2 orchestrator injection tests pass. Confirms active rules appear in injection string, proposed/empty returns fallback. No LLM, PostgreSQL, or MemoryService required.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green-check and MEM-04/MEM-05 sign-off</name>
  <files>tests/test_structured_memory.py</files>
  <action>
Run the full test suite for the structured memory module. If any test is failing due to import errors or test isolation issues (e.g., tmp files colliding between test classes), fix tearDown/setUp isolation — each class uses a distinct temp file path.

Also run the broader test suite to confirm no regressions:
  .venv/bin/python3.12 -m pytest tests/ -v --ignore=tests/test_order_router.py --ignore=tests/test_persistence.py -q

(test_order_router and test_persistence are pre-existing known failures unrelated to this phase.)

No production code changes are needed in this task — this is a verification task only. If a test failure points to a missing production fix (e.g., a method not yet implemented), fix it and document in the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_structured_memory.py -v && echo "STRUCTURED MEMORY: ALL PASS"</automated>
  </verify>
  <done>All tests in test_structured_memory.py pass (target: 14 total — 10 from 09-01 + 4 new integration tests). No regressions in the broader test suite.</done>
</task>

</tasks>

<verification>
.venv/bin/python3.12 -m pytest tests/test_structured_memory.py -v
Target: 14 tests, all pass.

MEM-04 verified: data/memory_registry.json is the primary rule store (Pydantic-validated JSON with id, type, condition, action, evidence, status, version, timestamps).
MEM-05 verified: update_status() enforces one-way lifecycle (proposed->active->deprecated, no reverse), version incremented on each transition, INFO-level log emitted and asserted.
</verification>

<success_criteria>
- 14 tests pass in tests/test_structured_memory.py
- RuleGenerator persist_rules() confirmed to write proposed rules to JSON registry
- Orchestrator confirmed to inject only active rules into prompts
- Full lifecycle round-trip: RuleGenerator writes proposed -> update_status promotes to active -> orchestrator injects it — verified by test
- No regressions in broader test suite
</success_criteria>

<output>
After completion, create `.planning/phases/09-structured-memory-registry/09-02-SUMMARY.md`
</output>
