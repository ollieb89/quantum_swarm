---
phase: 05-quant-alpha-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/test_quant_alpha_intelligence.py
autonomous: false
requirements:
  - ANALY-03
must_haves:
  truths:
    - "All 11 phase 5 tests pass without error"
    - "Skill 'quant-alpha-intelligence' is discoverable in the SkillRegistry"
    - "calculate_indicators tool is present in both MacroAnalyst and QuantModeler tool lists"
    - "A smoke test confirms calculate_indicators returns status 'ok' and a results dict when called with valid input"
  artifacts:
    - path: "src/skills/quant_alpha_intelligence.py"
      provides: "SKILL_INTENT = 'quant-alpha-intelligence' and handle() function"
      contains: "SKILL_INTENT"
    - path: "src/tools/analyst_tools.py"
      provides: "calculate_indicators @tool wrapping handle()"
      contains: "calculate_indicators"
    - path: "src/graph/agents/analysts.py"
      provides: "MacroAnalyst and QuantModeler with calculate_indicators in tool list"
      contains: "calculate_indicators"
  key_links:
    - from: "src/graph/agents/analysts.py"
      to: "src/tools/analyst_tools.py"
      via: "import calculate_indicators"
      pattern: "from src.tools.analyst_tools import.*calculate_indicators"
    - from: "src/tools/analyst_tools.py"
      to: "src/skills/quant_alpha_intelligence.py"
      via: "from src.skills.quant_alpha_intelligence import handle"
      pattern: "from src.skills.quant_alpha_intelligence import"
    - from: "src/skills/registry.py"
      to: "src/skills/quant_alpha_intelligence.py"
      via: "pkgutil.iter_modules discovers SKILL_INTENT"
      pattern: "SKILL_INTENT.*quant-alpha-intelligence"
---

<objective>
Verify the full integration chain for Phase 5: skill registration, tool exposure, agent wiring, and test suite completeness. This plan runs after 05-01 closes the spec gaps and confirms the phase success criteria are met end-to-end.

Purpose: Phase 6 (Stop-Loss Enforcement) depends on Phase 5 providing the ATR calculation. This verification confirms the dependency is satisfied before Phase 6 begins.

Output: Human-verified checkpoint confirming all phase criteria pass. Phase 5 SUMMARY written.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quant-alpha-intelligence/05-CONTEXT.md
@.planning/phases/05-quant-alpha-intelligence/05-01-SUMMARY.md

<interfaces>
<!-- Integration chain to verify -->

SkillRegistry discovery path:
  pkgutil.iter_modules(['src/skills/'])
    → finds quant_alpha_intelligence module
    → reads SKILL_INTENT = "quant-alpha-intelligence"
    → registers handle() as the intent handler
  registry.intents → contains "quant-alpha-intelligence"

Tool wiring:
  src/tools/analyst_tools.py:
    from src.skills.quant_alpha_intelligence import handle
    @tool calculate_indicators(series, indicators) → calls handle(state)

  src/graph/agents/analysts.py:
    from src.tools.analyst_tools import calculate_indicators
    _get_macro_agent() → tools=[..., calculate_indicators, ...]
    _get_quant_agent() → tools=[..., calculate_indicators, ...]

Phase 5 success criteria from ROADMAP.md:
  1. calculate_indicators with price series → RSI, MACD, BB values without error
  2. Skill registered in YAML skill registry, discoverable by L1 orchestrator
  3. QuantModeler can invoke skill via tool interface, receive structured output
  4. Unit tests cover each indicator calculation and pass against known reference values
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full phase 5 test suite and verify integration imports</name>
  <files>tests/test_quant_alpha_intelligence.py</files>
  <action>
Run the complete test suite and perform three import-level checks to confirm the integration chain is wired correctly. No code changes required if the integration is already correct — this is a verification task.

Step 1: Run the tests.
Command: `.venv/bin/python3.12 -m pytest tests/test_quant_alpha_intelligence.py -v`
Expected: 11 passed, 0 failed.

Step 2: Verify SkillRegistry discovers the skill.
Command: `.venv/bin/python3.12 -c "from src.skills.registry import SkillRegistry; r = SkillRegistry(); r.discover(); assert 'quant-alpha-intelligence' in r.intents, f'Missing skill, found: {r.intents}'; print('PASS: skill discovered')"`

Step 3: Verify calculate_indicators tool is importable and callable (no API key needed — tool is pure Python).
Command: `.venv/bin/python3.12 -c "from src.tools.analyst_tools import calculate_indicators; result = calculate_indicators.invoke({'series': {'close': [float(i) for i in range(101, 121)]}, 'indicators': [{'name': 'rsi', 'params': {'period': 14}}]}); assert result['status'] == 'ok'; assert 'rsi' in result['results']; print('PASS: calculate_indicators returns ok, rsi present')"`

Step 4: Verify analyst imports include calculate_indicators.
Command: `.venv/bin/python3.12 -c "import ast, sys; src = open('src/graph/agents/analysts.py').read(); assert 'calculate_indicators' in src, 'calculate_indicators not found in analysts.py'; print('PASS: calculate_indicators referenced in analysts.py')"`

If any step fails, diagnose and fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_quant_alpha_intelligence.py -v && .venv/bin/python3.12 -c "from src.skills.registry import SkillRegistry; r = SkillRegistry(); r.discover(); assert 'quant-alpha-intelligence' in r.intents; print('skill registry: OK')" && .venv/bin/python3.12 -c "from src.tools.analyst_tools import calculate_indicators; r = calculate_indicators.invoke({'series': {'close': [float(i) for i in range(101, 121)]}, 'indicators': [{'name': 'rsi', 'params': {'period': 14}}]}); assert r['status'] == 'ok'; print('tool invocation: OK')"</automated>
  </verify>
  <done>11 tests pass. SkillRegistry discovers "quant-alpha-intelligence". calculate_indicators.invoke() returns status "ok" with rsi result dict containing "value" and "state" keys.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of phase 5 success criteria</name>
  <what-built>
Phase 5 implementation: centralized technical indicator skill (RSI, MACD, Bollinger Bands, ATR) registered as calculate_indicators tool, wired to MacroAnalyst and QuantModeler.

Spec changes from 05-01:
- RSI output now returns {"value": float, "state": "overbought"|"oversold"|"neutral"} from handle()
- Error codes now distinguish INSUFFICIENT_DATA (too-short series) from INVALID_INPUT (param errors)
- 11 tests passing (up from 9)
  </what-built>
  <how-to-verify>
1. Run the test suite:
   `.venv/bin/python3.12 -m pytest tests/test_quant_alpha_intelligence.py -v`
   Expected: 11 passed, 0 failed

2. Confirm RSI state output:
   `.venv/bin/python3.12 -c "from src.skills.quant_alpha_intelligence import handle; r = handle({'series': {'close': [float(i) for i in range(101, 121)]}, 'indicators': [{'name': 'rsi', 'params': {'period': 14}}]}); print(r['results']['rsi'])"`
   Expected: {'value': <float>, 'state': 'overbought'|'oversold'|'neutral'}

3. Confirm INSUFFICIENT_DATA error code:
   `.venv/bin/python3.12 -c "from src.skills.quant_alpha_intelligence import handle; r = handle({'series': {'close': [100.0, 101.0]}, 'indicators': [{'name': 'rsi', 'params': {'period': 14}}]}); print(r['results']['rsi']['error'])"`
   Expected: {'code': 'INSUFFICIENT_DATA', 'message': '...requires at least...'}

4. Check that calculate_indicators is listed in both agent tool lists by inspecting analysts.py:
   `grep -n "calculate_indicators" src/graph/agents/analysts.py`
   Expected: at least 2 lines (one in each agent's tools list)
  </how-to-verify>
  <action>Run verification commands in how-to-verify and confirm each expected output matches.</action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_quant_alpha_intelligence.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms: 11 tests pass, RSI state field present, INSUFFICIENT_DATA error code returned for short series, calculate_indicators in both agent tool lists.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All phase 5 ROADMAP success criteria verified:

1. calculate_indicators with a price series returns RSI (with state), MACD, BB, and ATR values without error
2. "quant-alpha-intelligence" is in SkillRegistry.intents after discover()
3. Both MacroAnalyst and QuantModeler have calculate_indicators in their tool lists
4. 11 unit tests pass covering all four indicators and error handling paths
</verification>

<success_criteria>
- `.venv/bin/python3.12 -m pytest tests/test_quant_alpha_intelligence.py -v` → 11 passed
- SkillRegistry.discover() → "quant-alpha-intelligence" in intents
- calculate_indicators.invoke() with valid input → status "ok", results["rsi"]["state"] present
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-quant-alpha-intelligence/05-02-SUMMARY.md`
</output>
