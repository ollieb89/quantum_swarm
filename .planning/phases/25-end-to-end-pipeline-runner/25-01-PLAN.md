---
phase: 25-end-to-end-pipeline-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/logging_config.py
  - src/tools/data_sources/yfinance_client.py
  - src/core/soul_loader.py
  - tests/test_logging_config.py
  - tests/test_yfinance_resilience.py
  - tests/core/test_soul_loader.py
  - tests/core/test_import_boundaries.py
autonomous: true
requirements:
  - PIPE-02
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "structlog configures all existing getLogger calls to emit structured output via ProcessorFormatter"
    - "yfinance retries 3 times with exponential backoff on failure before raising"
    - "Disk cache writes on every successful yfinance fetch; reads only when QS_DEV_CACHE=1"
    - "reload_souls() clears lru_cache and re-warms all known agents"
  artifacts:
    - path: "src/core/logging_config.py"
      provides: "structlog stdlib integration configuration"
      exports: ["configure_logging"]
    - path: "tests/test_logging_config.py"
      provides: "structlog configuration tests"
      min_lines: 30
    - path: "tests/test_yfinance_resilience.py"
      provides: "retry + disk cache tests"
      min_lines: 50
  key_links:
    - from: "src/core/logging_config.py"
      to: "logging root handler"
      via: "ProcessorFormatter on root logger"
      pattern: "root\\.addHandler"
    - from: "src/tools/data_sources/yfinance_client.py"
      to: "data/cache/{symbol}.json"
      via: "disk cache write on fetch"
      pattern: "_write_disk_cache"
    - from: "src/core/soul_loader.py"
      to: "load_soul.cache_clear"
      via: "reload_souls function"
      pattern: "load_soul\\.cache_clear"
---

<objective>
Build the three infrastructure modules that the CLI entry point (Plan 02) depends on: structlog logging configuration, yfinance retry+cache resilience, and soul cache hot-reload.

Purpose: These are independent core modules with no cross-dependencies, enabling parallel development and isolated testing before the CLI wires them together.
Output: Three production-ready modules with full test coverage.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-end-to-end-pipeline-runner/25-CONTEXT.md
@.planning/phases/25-end-to-end-pipeline-runner/25-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/core/soul_loader.py:
```python
@lru_cache(maxsize=None)
def load_soul(agent_id: str) -> AgentSoul: ...
def warmup_soul_cache() -> None: ...
_KNOWN_AGENTS = ("macro_analyst", "bullish_researcher", "bearish_researcher", "quant_modeler", "risk_manager")
```

From src/tools/data_sources/yfinance_client.py:
```python
async def fetch_equity_data(symbol: str, period: str = "6mo") -> MarketData: ...
_data_cache: Dict[Tuple[str, str], MarketData] = {}
def clear_cache(): ...
```

From src/models/data_models.py:
```python
class MarketData(BaseModel):
    symbol: str; price: float; volume: float; open: float; high: float
    low: float; close: float; timestamp: datetime; source: str; interval: str
```

From tests/core/test_import_boundaries.py:
```python
class TestCoreLeafImports:
    # Add test_logging_config_imports_cleanly here
    def test_cycle_runner_imports_cleanly(self): _isolated_import("src.core.cycle_runner")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: structlog logging configuration + yfinance retry and disk cache</name>
  <files>src/core/logging_config.py, src/tools/data_sources/yfinance_client.py, tests/test_logging_config.py, tests/test_yfinance_resilience.py, tests/core/test_import_boundaries.py</files>
  <behavior>
    - Test: configure_logging() replaces root logger handler with structlog ProcessorFormatter
    - Test: LOG_FORMAT=json produces JSONRenderer output; default produces ConsoleRenderer
    - Test: Logs go to stderr (not stdout) so CycleSnapshot JSON output is uncontaminated
    - Test: logging_config.py imports cleanly as a core leaf module (import boundary test)
    - Test: yfinance fetch retries 3 times on exception with exponential backoff (1s, 2s, 4s base delays)
    - Test: After 3 failed retries, exception is raised (not swallowed)
    - Test: Successful fetch writes disk cache to data/cache/{symbol}.json with _cached_at timestamp
    - Test: With QS_DEV_CACHE=1, disk cache is read before hitting yfinance
    - Test: Without QS_DEV_CACHE=1, disk cache is written but never read
    - Test: Disk cache with expired TTL is ignored even with QS_DEV_CACHE=1
  </behavior>
  <action>
    1. Install structlog: `pip install structlog>=24.1.0`

    2. Create `src/core/logging_config.py` with `configure_logging()` function:
       - Read `LOG_FORMAT` env var (default "console")
       - shared_processors: add_log_level, add_logger_name, TimeStamper(fmt="iso"), ExtraAdder()
       - If LOG_FORMAT=json: use structlog.processors.JSONRenderer()
       - Else: use structlog.dev.ConsoleRenderer()
       - Create ProcessorFormatter with foreign_pre_chain=shared_processors
       - Create StreamHandler(sys.stderr) with the formatter
       - Clear root logger handlers, add new handler, set level INFO
       - Per research Pattern 1 — logs to stderr, stdout reserved for CycleSnapshot JSON

    3. Modify `src/tools/data_sources/yfinance_client.py`:
       - Add constants: MAX_RETRIES=3, BASE_DELAY=1.0, CACHE_DIR=Path("data/cache"), CACHE_TTL_SECONDS=3600
       - Add `_fetch_with_retry(symbol, period)` async function wrapping the yf.download call with retry loop: 3 attempts, delay = BASE_DELAY * 2^attempt + random.uniform(0, 0.5), use asyncio.sleep for delay
       - Add `_read_disk_cache(symbol)` function: returns None if QS_DEV_CACHE != "1", checks file existence and TTL, returns MarketData or None
       - Add `_write_disk_cache(symbol, market_data)` function: writes JSON with _cached_at timestamp to CACHE_DIR/{symbol}.json, mkdir parents
       - Modify `fetch_equity_data()`: check in-memory cache first (existing), then _read_disk_cache, then _fetch_with_retry (replacing direct yf.download call), then _write_disk_cache on success
       - Preserve existing multi-index column normalization (lines 57-61)

    4. Add `test_logging_config_imports_cleanly` to TestCoreLeafImports in `tests/core/test_import_boundaries.py`

    5. Create `tests/test_logging_config.py` with tests for configure_logging() behavior

    6. Create `tests/test_yfinance_resilience.py` with tests for retry, disk cache read/write, TTL expiry. Mock yf.download, mock time.time for TTL tests, use tmp_path for cache dir, use monkeypatch for QS_DEV_CACHE env var
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_logging_config.py tests/test_yfinance_resilience.py tests/core/test_import_boundaries.py::TestCoreLeafImports::test_logging_config_imports_cleanly -x -v</automated>
  </verify>
  <done>
    - logging_config.py exists with configure_logging() that wraps stdlib loggers via structlog ProcessorFormatter
    - yfinance_client.py retries 3 times with exponential backoff, writes disk cache always, reads disk cache only with QS_DEV_CACHE=1
    - logging_config.py passes import boundary test
    - All new tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Soul cache hot-reload + node timing in audit wrapper</name>
  <files>src/core/soul_loader.py, src/graph/orchestrator.py, tests/core/test_soul_loader.py</files>
  <behavior>
    - Test: reload_souls() calls load_soul.cache_clear() and then warmup_soul_cache()
    - Test: After reload_souls(), loading a soul returns fresh content (not stale cached version)
    - Test: with_audit_logging wrapper logs node_enter and node_exit with duration_ms field
  </behavior>
  <action>
    1. Add `reload_souls()` function to `src/core/soul_loader.py`:
       ```python
       def reload_souls() -> None:
           """Clear soul lru_cache and re-warm all known agents."""
           load_soul.cache_clear()
           warmup_soul_cache()
       ```

    2. Modify `with_audit_logging()` in `src/graph/orchestrator.py` to add wall-clock timing:
       - Import `time` at top of file (if not already)
       - Before node execution: `t0 = time.monotonic()` and `logger.info("node_enter", extra={"node": node_id})`
       - After node execution: compute elapsed_ms = round((time.monotonic() - t0) * 1000, 1) and `logger.info("node_exit", extra={"node": node_id, "duration_ms": elapsed_ms, "status": "ok"})`
       - On exception: log with status="error" before re-raising
       - Use `extra={}` dict pattern so structlog's ExtraAdder() picks up the fields

    3. Extend `tests/core/test_soul_loader.py` with test_reload_souls:
       - Load a soul, verify cached
       - Call reload_souls()
       - Verify cache was cleared (load_soul.cache_info().hits resets or similar)
       - Verify souls are re-loaded (cache_info shows all 5 loaded)
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/core/test_soul_loader.py -x -v -k "reload"</automated>
  </verify>
  <done>
    - reload_souls() exists in soul_loader.py, clears lru_cache and re-warms
    - with_audit_logging logs node_enter/node_exit with duration_ms via extra dict
    - test_reload_souls passes
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.12 -m pytest tests/test_logging_config.py tests/test_yfinance_resilience.py tests/core/test_soul_loader.py tests/core/test_import_boundaries.py -x -v`
- All new modules import cleanly without graph dependencies
- structlog is installed and importable
</verification>

<success_criteria>
- configure_logging() wraps all stdlib loggers with structlog (logs to stderr)
- yfinance_client.py has 3-retry exponential backoff + disk cache (write always, read only with QS_DEV_CACHE=1)
- reload_souls() clears and re-warms soul cache
- with_audit_logging emits node timing via structured log fields
- All tests pass, import boundaries clean
</success_criteria>

<output>
After completion, create `.planning/phases/25-end-to-end-pipeline-runner/25-01-SUMMARY.md`
</output>
