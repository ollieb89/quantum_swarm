---
phase: 04-memory-compliance
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/core/persistence.py
  - tests/test_institutional_guard.py
autonomous: true
requirements: [MEM-01, SEC-02, SEC-04, RISK-02]
gap_closure: true

must_haves:
  truths:
    - "exit_time TIMESTAMPTZ is present in the trades CREATE TABLE block in setup_persistence()"
    - "All three tests in test_institutional_guard.py pass without a live PostgreSQL connection"
    - "No test in test_institutional_guard.py asserts against a coroutine object"
    - "InstitutionalGuard concurrent-trades limit is covered by at least one passing test"
  artifacts:
    - path: "src/core/persistence.py"
      provides: "trades DDL with exit_time column"
      contains: "exit_time TIMESTAMPTZ"
    - path: "tests/test_institutional_guard.py"
      provides: "Three passing async-correct guard tests"
      exports: ["test_institutional_guard_restricted_asset", "test_institutional_guard_concurrent_trades", "test_institutional_guard_node_logic"]
  key_links:
    - from: "src/security/institutional_guard.py"
      to: "trades table"
      via: "_get_open_positions() query"
      pattern: "WHERE exit_time IS NULL"
    - from: "tests/test_institutional_guard.py"
      to: "InstitutionalGuard.check_compliance()"
      via: "asyncio.run()"
      pattern: "asyncio\\.run\\(guard\\.check_compliance"
---

<objective>
Close two confirmed bugs from the Phase 04 VERIFICATION.md that were identified in plan 04-01 but
whose fix commits were never executed against the codebase.

Gap 1 — Schema bug: The trades DDL in src/core/persistence.py is missing the `exit_time TIMESTAMPTZ`
column. InstitutionalGuard._get_open_positions() queries `WHERE exit_time IS NULL`, which will raise
`psycopg.errors.UndefinedColumn` against any live database.

Gap 2 — Vacuous tests: All three tests in tests/test_institutional_guard.py call async methods
without asyncio.run(). pytest captures TypeError: 'coroutine' object is not subscriptable on every
assertion — no compliance gate behaviour has ever been verified automatically.

Purpose: Restore meaningful automated coverage for SEC-02, SEC-04, and RISK-02 requirements, and
make the trades schema correct so InstitutionalGuard can execute against a live database without error.

Output:
  - src/core/persistence.py with exit_time TIMESTAMPTZ in the trades CREATE TABLE block
  - tests/test_institutional_guard.py fully rewritten with asyncio.run() and AsyncMock
  - All three guard tests passing; no live PostgreSQL required
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-memory-compliance/04-VERIFICATION.md

<interfaces>
<!-- Key types extracted from src/security/institutional_guard.py — executor uses these directly. -->

InstitutionalGuard.check_compliance() — what checks run and in what order:
  1. Restricted asset check (lines 87-88) — fires BEFORE _get_open_positions() is called.
     Returns: {"approved": False, "violation": "Asset XRP/USDT is restricted."}
     => Restricted-asset rejection tests do NOT need _get_open_positions mocked.

  2. _get_open_positions() called (line 91) — returns List[Dict] from PostgreSQL.
     => Any test that reaches this line needs AsyncMock on _get_open_positions.

  3. Concurrent trades check (lines 94-95):
     if len(open_positions) >= self.max_concurrent:  # default max_concurrent = 10
         return {"approved": False, "violation": "Max concurrent trades (10) reached."}
     => To trigger: patch _get_open_positions to return a list of 10 dicts.

  4. Approved path returns (lines 117-122):
     {"approved": True, "violation": None, "risk_score": float, "portfolio_heat": float}
     => To trigger: patch _get_open_positions to return [].

institutional_guard_node() — node-level wrapping (lines 124-148):
  Violation path sets: {"risk_approved": False, "risk_notes": "...", "compliance_flags": [...]}
  Approval path sets:  {"compliance_flags": [..., "INSTITUTIONAL_APPROVED"], "metadata": {...}}
  Note: approval path does NOT set risk_approved key at all.

Mocking pattern (AsyncMock avoids live PostgreSQL):
  from unittest.mock import AsyncMock, patch

  with patch.object(InstitutionalGuard, '_get_open_positions',
                    new_callable=AsyncMock, return_value=[]):
      result = asyncio.run(guard.check_compliance(state))

Project test pattern (Python 3.12, no pytest-asyncio):
  def test_my_async_function():
      result = asyncio.run(my_async_function(arg))
      assert result["key"] == expected

Current trades DDL (persistence.py lines 62-77) — exit_time absent:
  CREATE TABLE IF NOT EXISTS trades (
      ...
      execution_time TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
      -- exit_time TIMESTAMPTZ   <-- MISSING HERE at line 73
      execution_mode VARCHAR(16) NOT NULL,
      ...
  );
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exit_time column to trades DDL in persistence.py</name>
  <files>src/core/persistence.py</files>
  <action>
    In setup_persistence(), find the trades CREATE TABLE block. The current DDL at lines 62-77
    reads:

        execution_time TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
        execution_mode VARCHAR(16) NOT NULL,

    Insert `exit_time TIMESTAMPTZ,` between those two lines so the block becomes:

        execution_time TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
        exit_time TIMESTAMPTZ,
        execution_mode VARCHAR(16) NOT NULL,

    Also add a migration comment immediately above the CREATE TABLE IF NOT EXISTS line (line 61):

        # Migration note: for existing DBs run:
        # ALTER TABLE trades ADD COLUMN IF NOT EXISTS exit_time TIMESTAMPTZ;

    No other changes to this file. Do not touch audit_logs DDL, get_checkpointer(), or any
    imports. This is a single targeted DDL addition.
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -c "src=open('src/core/persistence.py').read(); assert 'exit_time TIMESTAMPTZ' in src, 'FAIL: exit_time column missing'; assert 'Migration note' in src, 'FAIL: migration comment missing'; print('OK: exit_time column present and migration comment added')"</automated>
  </verify>
  <done>
    src/core/persistence.py contains `exit_time TIMESTAMPTZ,` in the trades CREATE TABLE block,
    placed after execution_time. The migration comment appears above the CREATE TABLE statement.
    InstitutionalGuard._get_open_positions() will no longer raise UndefinedColumn at runtime.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_institutional_guard.py with asyncio.run() and AsyncMock</name>
  <files>tests/test_institutional_guard.py</files>
  <action>
    Replace the entire file content. The three test functions must be rewritten as follows.
    Do NOT add pytest-asyncio markers — it is not installed. Use plain `def` functions with
    asyncio.run() inside, per MEMORY.md.

    Required imports:
      import asyncio
      from unittest.mock import AsyncMock, patch
      from src.security.institutional_guard import InstitutionalGuard, institutional_guard_node

    ---

    test_institutional_guard_restricted_asset:
      - Restricted asset path: symbol "XRP/USDT" is in restricted_assets list.
        check_compliance returns before _get_open_positions() is called (line 87-88 of
        institutional_guard.py). No mock needed for this path.
        Assert: result["approved"] is False, "restricted" in result["violation"].

      - Allowed asset path: symbol "BTC/USDT" is not restricted. _get_open_positions()
        IS called. Mock it to return [] (empty — no open positions).
        Assert: result["approved"] is True.

    ---

    test_institutional_guard_concurrent_trades (replaces the old leverage test):
      The current check_compliance() has NO leverage check. Replace with a concurrent-trades
      test that exercises the portfolio limit.

      config = {"risk_limits": {"max_concurrent_trades": 10}}
      guard = InstitutionalGuard(config)
      state = {"quant_proposal": {"symbol": "BTC/USDT"}}

      Exceeded path: patch _get_open_positions to return a list of 10 identical dicts
        [{"symbol": "ETH/USDT", "quantity": 1.0, "price": 1000.0}] * 10
        len(open_positions) >= max_concurrent (10 >= 10) triggers the violation.
        Assert: result["approved"] is False, "Max concurrent trades" in result["violation"].

      Under limit path: patch _get_open_positions to return [] (0 open positions).
        Assert: result["approved"] is True.

    ---

    test_institutional_guard_node_logic:
      config = {"risk_limits": {"restricted_assets": ["BAD/USDT"]}}

      Violation path: symbol "BAD/USDT" is restricted. Fires before _get_open_positions().
        No mock needed. Use asyncio.run(institutional_guard_node(state, config)).
        state = {
            "quant_proposal": {"symbol": "BAD/USDT"},
            "compliance_flags": ["PRE_EXISTING"],
            "risk_notes": "All good so far"
        }
        Assert:
          update["risk_approved"] is False
          any("INSTITUTIONAL_VIOLATION" in f for f in update["compliance_flags"])
          "Institutional Guard Block" in update["risk_notes"]

      Approval path: symbol "GOOD/USDT" is not restricted. _get_open_positions() IS called.
        Mock it to return [].
        state = {"quant_proposal": {"symbol": "GOOD/USDT"}, "compliance_flags": []}
        Assert:
          "INSTITUTIONAL_APPROVED" in update["compliance_flags"]
          "risk_approved" not in update  (node only sets risk_approved on violation)
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_institutional_guard.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    All three tests pass: test_institutional_guard_restricted_asset,
    test_institutional_guard_concurrent_trades, test_institutional_guard_node_logic.
    No TypeError about coroutine objects. Tests run without a live PostgreSQL container.
    `.venv/bin/python3.12 -m pytest tests/test_institutional_guard.py -v` exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks in sequence:

```bash
# 1. Confirm exit_time column in DDL
.venv/bin/python3.12 -c "
src = open('src/core/persistence.py').read()
assert 'exit_time TIMESTAMPTZ' in src, 'FAIL: column missing'
assert 'Migration note' in src, 'FAIL: migration comment missing'
print('DDL fix: OK')
"

# 2. Confirm all three guard tests pass
.venv/bin/python3.12 -m pytest tests/test_institutional_guard.py -v

# 3. Confirm no regressions in the broader in-memory test suite
.venv/bin/python3.12 -m pytest tests/test_institutional_guard.py tests/test_inter_agent_blackboard.py tests/test_budget_tracking.py tests/test_memory.py tests/test_memory_nodes.py -x -q
```

Expected: All targeted tests pass, no regressions in the broader non-DB test suite.
</verification>

<success_criteria>
1. src/core/persistence.py trades DDL contains `exit_time TIMESTAMPTZ` — the InstitutionalGuard
   _get_open_positions() query will no longer raise UndefinedColumn at runtime.
2. tests/test_institutional_guard.py — all 3 tests pass using asyncio.run() and assert against
   real dict results (not coroutine objects).
3. The leverage test is replaced by a meaningful concurrent-trades test that exercises the actual
   production check_compliance() code path.
4. No tests that were previously passing are now broken.
5. Requirements SEC-02, SEC-04, and RISK-02 each have at least one passing automated test.
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-compliance/04-03-SUMMARY.md` documenting:
- The two bugs fixed and why the original commits were never made
- The corrected DDL and the migration comment rationale
- The test rewrite: which async patterns were used, what the leverage test was replaced with and why
- Final test counts and pass/fail status
</output>
