---
phase: 27-environment-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - tests/test_data_fetcher.py
  - tests/test_calibration.py
  - tests/test_dexter_bridge.py
  - tests/test_audit_chain.py
  - tests/test_persistence.py
  - tests/test_trade_logger.py
  - tests/test_knowledge_base.py
autonomous: true
requirements:
  - ENV-03
  - ENV-04

must_haves:
  truths:
    - "All async tests run correctly without @pytest.mark.asyncio decorators (auto mode handles detection)"
    - "Full test suite passes with 0 environment-related failures"
    - "PostgreSQL-dependent tests are isolated so they do not count as env failures"
  artifacts:
    - path: "tests/test_data_fetcher.py"
      provides: "Async tests without redundant decorators"
    - path: "tests/test_calibration.py"
      provides: "Async tests without redundant decorators"
    - path: "tests/test_dexter_bridge.py"
      provides: "Async tests without redundant decorators"
    - path: "tests/test_trade_logger.py"
      provides: "Async tests without redundant decorators"
    - path: "tests/test_knowledge_base.py"
      provides: "Async tests without redundant decorators"
  key_links:
    - from: "pyproject.toml"
      to: "all async test files"
      via: "asyncio_mode = auto"
      pattern: "asyncio_mode.*auto"
---

<objective>
Strip redundant @pytest.mark.asyncio decorators and verify full test suite is green.

Purpose: With `asyncio_mode = "auto"` already configured in pyproject.toml, the @pytest.mark.asyncio decorators are redundant. Remove them from all 7 test files (19 occurrences). Then run the full suite to confirm 0 environment-related failures, completing ENV-03 and ENV-04.
Output: Clean test files and a verified green test suite.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-environment-stabilization/27-CONTEXT.md
@.planning/phases/27-environment-stabilization/27-RESEARCH.md
@.planning/phases/27-environment-stabilization/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip @pytest.mark.asyncio decorators from all test files</name>
  <files>tests/test_data_fetcher.py, tests/test_calibration.py, tests/test_dexter_bridge.py, tests/test_audit_chain.py, tests/test_persistence.py, tests/test_trade_logger.py, tests/test_knowledge_base.py</files>
  <action>
Remove all `@pytest.mark.asyncio` decorator lines from these 7 files (19 occurrences total). The `asyncio_mode = "auto"` in pyproject.toml auto-detects `async def test_*` functions, making these decorators redundant.

Specific counts per file:
- `tests/test_data_fetcher.py`: 5 decorators (lines 37, 80, 131, 153, 196)
- `tests/test_calibration.py`: 4 decorators (lines 40, 70, 119, 141)
- `tests/test_dexter_bridge.py`: 3 decorators (lines 17, 44, 67)
- `tests/test_audit_chain.py`: 2 decorators (lines 20, 52)
- `tests/test_persistence.py`: 2 decorators (lines 9, 46)
- `tests/test_trade_logger.py`: 2 decorators (lines 6, 75)
- `tests/test_knowledge_base.py`: 1 decorator (line 9)

Also remove `import pytest` from any file where it was ONLY used for the `@pytest.mark.asyncio` decorator and has no other pytest usage (e.g., `pytest.raises`, `pytest.fixture`, `pytest.mark.skipif`). Check each file before removing the import.

Do NOT remove `@pytest.mark.asyncio` from any files outside the `tests/` directory.
  </action>
  <verify>
    <automated>grep -rc "@pytest.mark.asyncio" tests/ && echo "FAIL: decorators remain" || echo "PASS: all decorators removed"</automated>
  </verify>
  <done>All 19 @pytest.mark.asyncio decorators removed from 7 test files. No redundant pytest imports left.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite green with 0 env-related failures</name>
  <files></files>
  <action>
Run the full test suite and verify results:

1. Run: `.venv/bin/python3.12 -m pytest --tb=short -q`
2. Confirm that async tests still execute (test count should not drop -- expect 681+ passed)
3. The only acceptable failures are PostgreSQL-dependent tests (test_persistence.py, test_audit_chain.py) which require a running PG instance -- these are infrastructure tests, not environment dependency issues
4. If any previously-passing test now fails, diagnose and fix immediately -- it is likely a patch target issue in test_data_fetcher.py after the ccxt lazy init change from Plan 01

If PostgreSQL tests are the only failures (as expected), optionally mark them with `@pytest.mark.skipif` using a PG availability check so the suite can run fully green in environments without PostgreSQL. This is within Claude's Discretion per CONTEXT.md. If doing this, add to test_persistence.py and test_audit_chain.py:
```python
import os
PG_AVAILABLE = bool(os.getenv("DB_URL"))
pytestmark = pytest.mark.skipif(not PG_AVAILABLE, reason="PostgreSQL not available")
```
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest --ignore=tests/test_persistence.py --ignore=tests/test_audit_chain.py -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite (excluding PG infra tests) passes with 0 failures. Test count is equal to or greater than pre-phase baseline (681). No environment-related test failures remain.</done>
</task>

</tasks>

<verification>
- `grep -rc "@pytest.mark.asyncio" tests/` returns 0 or "no match"
- `.venv/bin/python3.12 -m pytest --ignore=tests/test_persistence.py --ignore=tests/test_audit_chain.py -q` shows 0 failures
- Test count has not dropped (681+ tests collected)
- No Pydantic deprecation warnings in test output
</verification>

<success_criteria>
- All @pytest.mark.asyncio decorators removed (19 total from 7 files)
- Full test suite passes with 0 environment-related failures
- Async tests still execute correctly under auto mode
- Test count stable or increased vs baseline (681)
</success_criteria>

<output>
After completion, create `.planning/phases/27-environment-stabilization/27-02-SUMMARY.md`
</output>
