---
phase: 02-l2-domain-managers
plan: "02-05"
type: execute
wave: 5
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - tests/test_adversarial_debate.py
autonomous: true
requirements:
  - REQ-02-05
must_haves:
  truths:
    - Scenario A (overfitting) test verifies BearishResearcher lowers consensus on bad macro data
    - Scenario B (budget) test verifies system halts after 5 researcher fetches
    - Scenario C (provenance) test verifies SwarmState contains hypothesis for every researcher fetch
    - All tests pass with pytest
  artifacts:
    - tests/test_adversarial_debate.py
  key_links:
    - Tests use mocked LLM responses to avoid real API calls
    - Tests exercise the full graph path from analyst input to DebateSynthesizer output
---

<objective>
Implement integration tests for the adversarial debate layer, covering the three validation scenarios from the phase-2 plan: overfitting detection, budget enforcement, and provenance tracking.

Purpose: Validates that the full Phase 2 implementation behaves correctly end-to-end without requiring real API calls.
Output: tests/test_adversarial_debate.py
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASES/phase-2.md
@src/graph/state.py
@src/graph/orchestrator.py
@src/graph/debate.py
@src/graph/agents/analysts.py
@src/graph/agents/researchers.py
@src/tools/verification_wrapper.py
@tests/test_verification_wrapper.py
@tests/test_risk_gating.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scenario A — Overfitting detection test</name>
  <files>tests/test_adversarial_debate.py</files>
  <action>
    Create tests/test_adversarial_debate.py. Add test_scenario_a_overfitting:

    Setup:
    - Provide a SwarmState where MacroAnalyst output shows high-confidence quant signal but weak macro data (mock messages in state["messages"])
    - Mock the BullishResearcher LLM to return supporting evidence (low quality)
    - Mock the BearishResearcher LLM to return macro counter-evidence (calls fetch_economic_data with hypothesis="macro_refutation")

    Assertion:
    - After running DebateSynthesizer on the state, weighted_consensus_score < 0.5
    - The BearishResearcher's fetch_economic_data call appears in debate_history

    Use unittest.mock.patch to avoid real LLM/API calls.
  </action>
  <verify>pytest tests/test_adversarial_debate.py::test_scenario_a_overfitting -v 2>&1 | tail -5</verify>
  <done>test_scenario_a_overfitting passes</done>
</task>

<task type="auto">
  <name>Task 2: Scenario B — Budget enforcement test</name>
  <files>tests/test_adversarial_debate.py</files>
  <action>
    Add test_scenario_b_budget_enforcement:

    Setup:
    - Create a BudgetedTool wrapping a mock fetch_market_data (max_calls=5)
    - Simulate a researcher making 6 tool calls with hypothesis="budget_test"

    Assertion:
    - The 6th call raises ToolBudgetExceeded (imported from src.tools.verification_wrapper)
    - Calls 1-5 succeed and return the mock result

    This test focuses on the wrapper in isolation (unit-level), not full graph invocation.
  </action>
  <verify>pytest tests/test_adversarial_debate.py::test_scenario_b_budget_enforcement -v 2>&1 | tail -5</verify>
  <done>test_scenario_b_budget_enforcement passes</done>
</task>

<task type="auto">
  <name>Task 3: Scenario C — Provenance tracking test</name>
  <files>tests/test_adversarial_debate.py</files>
  <action>
    Add test_scenario_c_provenance:

    Setup:
    - Build a minimal SwarmState with mocked researcher output messages that include hypothesis metadata
    - Run DebateSynthesizer on the state

    Assertion:
    - Every entry in debate_history that originated from a researcher tool call has a non-empty "hypothesis" field
    - No researcher message in debate_history has hypothesis == None or hypothesis == ""

    This verifies that the hypothesis gating from verification_wrapper propagates into the state correctly.
  </action>
  <verify>pytest tests/test_adversarial_debate.py::test_scenario_c_provenance -v 2>&1 | tail -5</verify>
  <done>test_scenario_c_provenance passes</done>
</task>

<task type="auto">
  <name>Task 4: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
    Run the complete test suite to verify Phase 2 tests pass alongside existing tests:
    pytest tests/ -v --tb=short 2>&1

    If any pre-existing tests fail, investigate whether it's due to SwarmState schema changes made in Plans 02-03/02-04. Fix the regression (add default values for new fields) — do NOT skip or delete existing tests.
  </action>
  <verify>pytest tests/ -v --tb=short 2>&1 | tail -20</verify>
  <done>All tests pass (or pre-existing failures are documented with root cause)</done>
</task>

</tasks>

<verification>
- [ ] tests/test_adversarial_debate.py exists with 3 scenario tests
- [ ] pytest tests/test_adversarial_debate.py passes (all 3 scenarios)
- [ ] pytest tests/ -v passes with no new regressions introduced by Phase 2
</verification>
