---
phase: 02-l2-domain-managers
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/agents/analysts.py
  - src/tools/analyst_tools.py
autonomous: true
requirements:
  - REQ-02-01
must_haves:
  truths:
    - MacroAnalyst and QuantModeler are wrapped as LangGraph ReAct agents
    - L3 tools (fetch_market_data, run_backtest, fetch_economic_data) are registered
    - Agents use claude-haiku-4-5-20251001 model
  artifacts:
    - src/graph/agents/analysts.py
    - src/tools/analyst_tools.py
  key_links:
    - analysts.py imports tool functions from analyst_tools.py
    - Both agents are importable and instantiable
---

<objective>
Implement L2 Analyst Agents: wrap MacroAnalyst and QuantModeler as LangGraph ReAct agents with access to L3 executor tools.

Purpose: Provides the first layer of market analysis. Analyst agents form the inputs to the adversarial debate layer (Plan 02-02 and 02-03).
Output: src/graph/agents/analysts.py and src/tools/analyst_tools.py
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASES/phase-2.md
@src/graph/state.py
@src/agents/__init__.py
@src/agents/l3_executor.py
@src/graph/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analyst_tools.py with L3 tool wrappers</name>
  <files>src/tools/__init__.py, src/tools/analyst_tools.py</files>
  <action>
    Create src/tools/ package. In analyst_tools.py, define three LangGraph-compatible tool functions:
    - fetch_market_data(symbol: str, timeframe: str) -> dict
    - run_backtest(strategy: str, params: dict) -> dict
    - fetch_economic_data(indicator: str, region: str) -> dict

    Each function should delegate to the existing L3Executor where possible, or stub with realistic return shapes if L3Executor does not yet expose them. Decorate each with @tool from langchain_core.tools. Include clear docstrings describing inputs/outputs so the LLM can select them correctly.
  </action>
  <verify>python -c "from src.tools.analyst_tools import fetch_market_data, run_backtest, fetch_economic_data; print('Tools OK')"</verify>
  <done>All three tool functions import without error and are decorated as LangChain tools</done>
</task>

<task type="auto">
  <name>Task 2: Create analysts.py with MacroAnalyst and QuantModeler ReAct agents</name>
  <files>src/graph/agents/__init__.py, src/graph/agents/analysts.py</files>
  <action>
    Create src/graph/agents/ package. In analysts.py:
    - Import the three analyst tools from src.tools.analyst_tools
    - Build a MacroAnalyst node function and a QuantModeler node function using create_react_agent from langgraph.prebuilt
    - Use ChatAnthropic with model="claude-haiku-4-5-20251001" for both agents
    - MacroAnalyst uses: [fetch_market_data, fetch_economic_data]
    - QuantModeler uses: [fetch_market_data, run_backtest]
    - Each node function signature: (state: SwarmState) -> dict
    - Agents must write their outputs back into state["messages"] using AIMessage
    - Do NOT modify orchestrator.py in this plan — that happens in 02-03
  </action>
  <verify>python -c "from src.graph.agents.analysts import MacroAnalyst, QuantModeler; print('Agents OK')"</verify>
  <done>MacroAnalyst and QuantModeler are importable node functions that accept SwarmState and return a dict</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test both agents instantiate without API calls</name>
  <files>src/graph/agents/analysts.py</files>
  <action>
    Add a __main__ block to analysts.py that:
    - Creates a minimal SwarmState dict (task_id="test", intent="test", messages=[])
    - Calls MacroAnalyst(state) with a mock/patched LLM (use unittest.mock.patch or a fake model)
    - Verifies the return is a dict with a "messages" key
    This is a quick dev-loop sanity check, not a full integration test.
  </action>
  <verify>python -m src.graph.agents.analysts 2>&1 | grep -v "^$"</verify>
  <done>Script runs without import errors and prints a smoke-test result</done>
</task>

</tasks>

<verification>
- [ ] src/tools/analyst_tools.py exists with 3 @tool-decorated functions
- [ ] src/graph/agents/analysts.py exists with MacroAnalyst and QuantModeler node functions
- [ ] Both modules import cleanly: python -c "from src.graph.agents.analysts import MacroAnalyst, QuantModeler"
- [ ] Model used is claude-haiku-4-5-20251001 (grep the file to confirm)
</verification>
