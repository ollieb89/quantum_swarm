---
phase: 02-l2-domain-managers
plan: "02-03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/graph/debate.py
  - src/graph/orchestrator.py
autonomous: true
requirements:
  - REQ-02-03
must_haves:
  truths:
    - DebateSynthesizer node exists in src/graph/debate.py
    - Orchestrator routes analyst outputs to both researchers in parallel (fan-out)
    - DebateSynthesizer computes weighted_consensus_score and writes it to SwarmState
    - Graph compiles without error after orchestrator changes
  artifacts:
    - src/graph/debate.py
  key_links:
    - orchestrator.py adds fan-out edges to BullishResearcher and BearishResearcher
    - orchestrator.py adds DebateSynthesizer node after researcher fan-in
    - debate.py DebateSynthesizer reads bullish/bearish messages from state
---

<objective>
Implement Debate Synthesis: wire the fan-out/fan-in debate pattern into the orchestrator and implement the DebateSynthesizer node that computes a weighted consensus score.

Purpose: Integrates analysts and researchers into a coherent debate graph. Produces a consensus_score that downstream risk gating uses to decide whether to execute.
Output: src/graph/debate.py (new), src/graph/orchestrator.py (modified)
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASES/phase-2.md
@src/graph/state.py
@src/graph/orchestrator.py
@src/graph/agents/analysts.py
@src/graph/agents/researchers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consensus_score field to SwarmState</name>
  <files>src/graph/state.py</files>
  <action>
    Add the following fields to SwarmState TypedDict in src/graph/state.py:
    - weighted_consensus_score: Optional[float] = None  (0.0 = fully bearish, 1.0 = fully bullish)
    - debate_history: Annotated[List[dict], operator.add]  (accumulates all debate messages with provenance)

    Do not remove any existing fields. Keep all existing Annotated reducers intact.
  </action>
  <verify>python -c "from src.graph.state import SwarmState; s = SwarmState.__annotations__; print('consensus_score' in s, 'debate_history' in s)"</verify>
  <done>weighted_consensus_score and debate_history appear in SwarmState annotations</done>
</task>

<task type="auto">
  <name>Task 2: Create debate.py with DebateSynthesizer node</name>
  <files>src/graph/debate.py</files>
  <action>
    Create src/graph/debate.py with a DebateSynthesizer node function:

    Signature: (state: SwarmState) -> dict

    Logic:
    1. Extract bullish_research and bearish_research messages from state["messages"] by their source tag
    2. Count the number of supporting vs refuting evidence points (simple heuristic: count tool calls or argument length if LLM output is plain text)
    3. Compute weighted_consensus_score = bullish_strength / (bullish_strength + bearish_strength), clipped to [0.0, 1.0]
    4. Compile all debate messages into debate_history with provenance metadata (source, hypothesis, evidence)
    5. Return {"weighted_consensus_score": score, "debate_history": debate_history_entries}

    Keep the scoring logic simple and deterministic — no LLM call in the synthesizer itself (it's a pure aggregation step).
  </action>
  <verify>python -c "from src.graph.debate import DebateSynthesizer; print('Synthesizer OK')"</verify>
  <done>DebateSynthesizer imports and accepts a SwarmState dict, returning a dict with weighted_consensus_score</done>
</task>

<task type="auto">
  <name>Task 3: Wire fan-out and fan-in into orchestrator.py</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Modify src/graph/orchestrator.py to add the debate subgraph:

    1. Import MacroAnalyst, QuantModeler from src.graph.agents.analysts
    2. Import BullishResearcher, BearishResearcher from src.graph.agents.researchers
    3. Import DebateSynthesizer from src.graph.debate
    4. Add nodes to the StateGraph: "macro_analyst", "quant_modeler", "bullish_researcher", "bearish_researcher", "debate_synthesizer"
    5. Add fan-out edges:
       - After macro_analyst and quant_modeler complete -> route to both bullish_researcher and bearish_researcher in parallel (use Send or parallel edges pattern for LangGraph)
    6. Add fan-in: both researchers -> debate_synthesizer
    7. Do NOT add risk_manager or execute edges yet — that is plan 02-04

    Preserve all existing L1 nodes and routing logic. Only add new nodes/edges; do not remove existing ones.
  </action>
  <verify>python -c "from src.graph.orchestrator import build_graph; g = build_graph(); print('Graph OK')"</verify>
  <done>build_graph() compiles without error and graph includes the 5 new nodes</done>
</task>

</tasks>

<verification>
- [ ] SwarmState has weighted_consensus_score and debate_history fields
- [ ] src/graph/debate.py exists with DebateSynthesizer node
- [ ] orchestrator.py imports all 5 new nodes and wires fan-out/fan-in
- [ ] python -c "from src.graph.orchestrator import build_graph; build_graph()" completes without error
</verification>
