---
phase: 02-l2-domain-managers
plan: "02-04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - src/graph/orchestrator.py
autonomous: true
requirements:
  - REQ-02-04
must_haves:
  truths:
    - RiskManager is a LangGraph node in orchestrator.py
    - Conditional edge routes debate_synthesizer -> risk_manager only if consensus_score > 0.6
    - RiskManager receives full debate_history (provenance) from SwarmState
    - If consensus <= 0.6, graph routes to a rejection/hold node (not execute)
    - Graph compiles and conditional routing is exercised by a unit test
  artifacts: []
  key_links:
    - debate_synthesizer -> risk_manager conditional edge keyed on weighted_consensus_score
    - risk_manager -> execute edge (downstream, for Phase 3)
---

<objective>
Implement Risk Gating: migrate RiskManager to a LangGraph node and add a conditional edge from DebateSynthesizer that enforces the consensus threshold (>0.6) before allowing execution.

Purpose: Prevents low-confidence trades from reaching the execution layer. RiskManager receives full provenance (debate_history) for final validation.
Output: src/graph/orchestrator.py (modified)
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASES/phase-2.md
@src/graph/state.py
@src/graph/orchestrator.py
@src/graph/debate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RiskManager node in orchestrator.py</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Add a RiskManager node function (inline in orchestrator.py or in a new src/graph/agents/risk.py — your choice):

    Signature: (state: SwarmState) -> dict

    Logic:
    1. Read state["debate_history"] for full provenance
    2. Read state["weighted_consensus_score"]
    3. Perform final risk validation (check for conflicting hypotheses, missing provenance, anomalous score)
    4. Return {"risk_approved": True/False, "risk_notes": str}

    Add risk_approved: Optional[bool] and risk_notes: Optional[str] to SwarmState if not already present.
  </action>
  <verify>python -c "from src.graph.orchestrator import build_graph; print('RiskManager node present')"</verify>
  <done>RiskManager is a registered node in the graph and SwarmState has risk_approved field</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional edge from DebateSynthesizer to RiskManager</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    In the StateGraph setup:
    1. Define a routing function: route_after_debate(state: SwarmState) -> str
       - Returns "risk_manager" if state["weighted_consensus_score"] > 0.6
       - Returns "hold" (END or a hold node) otherwise
    2. Add graph.add_conditional_edges("debate_synthesizer", route_after_debate, {"risk_manager": "risk_manager", "hold": END})
    3. Add edge: risk_manager -> END (placeholder until Phase 3 adds execute node)

    The graph must still compile after this change. Existing L1 routing must be preserved.
  </action>
  <verify>python -c "from src.graph.orchestrator import build_graph; g = build_graph(); print('Conditional edge OK')"</verify>
  <done>Graph compiles; conditional edge exists on debate_synthesizer with threshold routing</done>
</task>

<task type="auto">
  <name>Task 3: Unit test for conditional routing threshold</name>
  <files>tests/test_risk_gating.py</files>
  <action>
    Create tests/test_risk_gating.py with pytest tests:

    1. test_high_consensus_routes_to_risk: Build a minimal state with weighted_consensus_score=0.8. Invoke route_after_debate(state). Assert result == "risk_manager".
    2. test_low_consensus_routes_to_hold: State with score=0.4. Assert result == "hold".
    3. test_boundary_excluded: State with score=0.6 (exactly). Assert result == "hold" (threshold is STRICT >0.6).

    Import route_after_debate directly from src.graph.orchestrator.
  </action>
  <verify>pytest tests/test_risk_gating.py -v 2>&1 | tail -10</verify>
  <done>All 3 routing tests pass</done>
</task>

</tasks>

<verification>
- [ ] SwarmState has risk_approved and risk_notes fields
- [ ] RiskManager node is registered in the StateGraph
- [ ] Conditional edge on debate_synthesizer routes based on weighted_consensus_score > 0.6
- [ ] pytest tests/test_risk_gating.py passes (3 tests)
- [ ] Graph compiles end-to-end: python -c "from src.graph.orchestrator import build_graph; build_graph()"
</verification>
