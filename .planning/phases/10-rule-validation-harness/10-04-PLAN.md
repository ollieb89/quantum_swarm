---
plan: 10-04
phase: 10
title: "Register MEM-06 in REQUIREMENTS.md (Gap Closure)"
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
mode: gap_closure
gap_source: 10-VERIFICATION.md
---

# Plan 10-04: Register MEM-06 in REQUIREMENTS.md

**Gap:** MEM-06 is cited in ROADMAP.md and all three Phase 10 plans but is entirely absent from `.planning/REQUIREMENTS.md`. No formal definition, no traceability entry. Implementation is complete; this is a documentation gap only.

**Fix:** Add MEM-06 to REQUIREMENTS.md under a new v1.3 section and register it in the Traceability table.

---

<tasks>

<task id="10-04-01" type="edit">
  <title>Add v1.3 requirements section with MEM-06</title>
  <file>.planning/REQUIREMENTS.md</file>
  <action>
    Insert a new `## v1.3 Requirements` section immediately after the `## v1.2 Requirements` section (before `## Future Requirements`).

    The new section content:

    ```markdown
    ## v1.3 Requirements

    Requirements for the Rule Validation Harness milestone. Each maps to roadmap phases.

    ### Memory / Self-Improvement (MEM)

    - [x] **MEM-06**: Proposed memory rules are automatically backtested before promotion; rules only transition to active if they pass a 2-of-3 metric evaluation harness (Sharpe ratio delta, max drawdown delta, win rate delta). Failing rules are moved to rejected. All promotion/rejection events are appended to `data/audit.jsonl` with full metric evidence.

    ---
    ```
  </action>
  <verification>
    `grep -n "MEM-06" .planning/REQUIREMENTS.md` returns at least one line in the v1.3 section.
  </verification>
</task>

<task id="10-04-02" type="edit">
  <title>Add MEM-06 to v1.3 Traceability table</title>
  <file>.planning/REQUIREMENTS.md</file>
  <action>
    Add a `### v1.3 Requirements` subsection to the `## Traceability` section, after the v1.2 traceability block and before the closing `---` line.

    The new subsection:

    ```markdown
    ### v1.3 Requirements

    | Requirement | Phase | Status |
    |-------------|-------|--------|
    | MEM-06 | Phase 10 | Complete |

    **Coverage:**
    - v1.3 requirements: 1 total
    - Mapped to phases: 1
    - Unmapped: 0 ✓
    ```
  </action>
  <verification>
    `grep -n "MEM-06" .planning/REQUIREMENTS.md` returns a line in the Traceability section.
    `grep -c "MEM-06" .planning/REQUIREMENTS.md` returns 2 (definition + traceability).
  </verification>
</task>

<task id="10-04-03" type="edit">
  <title>Update REQUIREMENTS.md footer timestamp</title>
  <file>.planning/REQUIREMENTS.md</file>
  <action>
    Replace the last line:
    ```
    *Last updated: 2026-03-07 — v1.2 added (MEM-04, MEM-05); former MEM-04 (regime-aware vector memory) renumbered to MEM-07*
    ```
    With:
    ```
    *Last updated: 2026-03-08 — v1.3 added (MEM-06: Rule Validation Harness, Phase 10 Complete)*
    ```
  </action>
  <verification>
    `grep "2026-03-08" .planning/REQUIREMENTS.md` returns the updated footer line.
  </verification>
</task>

</tasks>

---

## Verification Criteria

```bash
# MEM-06 appears in both definition and traceability
grep -c "MEM-06" .planning/REQUIREMENTS.md
# Expected: 2

# v1.3 section exists
grep "## v1.3 Requirements" .planning/REQUIREMENTS.md
# Expected: match

# Traceability entry maps to Phase 10 Complete
grep -A3 "MEM-06" .planning/REQUIREMENTS.md | grep "Phase 10"
# Expected: match
```

---

## must_haves

- [ ] `MEM-06` appears in `.planning/REQUIREMENTS.md` with a formal description under a v1.3 section
- [ ] `MEM-06` appears in the Traceability table mapped to Phase 10 with status Complete
- [ ] No other content in REQUIREMENTS.md is modified
- [ ] Footer timestamp updated to 2026-03-08

---

*Gap source: 10-VERIFICATION.md — "MEM-06 formally registered in REQUIREMENTS.md" (FAILED)*
*Fix type: Documentation only — no code changes required*
