---
phase: 01-foundation-orchestration-l1
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/skills/registry.py, src/graph/orchestrator.py]
autonomous: true
requirements: [ORCH-03, ORCH-04]

must_haves:
  truths:
    - "New skills are automatically discovered via YAML metadata"
    - "Sub-millisecond procedural tasks bypass LLM reasoning via direct command-dispatch"
    - "L1 agent only loads metadata for semantically relevant skills"
  artifacts:
    - path: "src/skills/registry.py"
      provides: "Automated skill discovery and management"
    - path: "src/graph/orchestrator.py"
      provides: "Deterministic bypass routing"
  key_links:
    - from: "src/graph/orchestrator.py"
      to: "src/skills/registry.py"
      via: "Skill metadata lookup"
---

<objective>
Implement automated skill discovery using YAML metadata and deterministic command dispatch to bypass LLM for procedural tasks. This optimizes performance and enables extensible agent capabilities.

Purpose: Reduce latency for procedural tasks and enable progressive discovery of specialized skills.
Output: Skill registry and deterministic bypass mechanism.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
@/home/ollie/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/graph/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Skill Registry with YAML Discovery</name>
  <files>src/skills/registry.py</files>
  <action>
    Create src/skills/registry.py with a SkillRegistry class.
    Implement scan_skills() to recursively find SKILL.md files in src/skills/.
    Parse YAML frontmatter for skill metadata: name, intent_triggers, description, and execution_command.
    Implement get_skill_by_intent(intent) and get_skill_metadata_for_prompts() for progressive disclosure.
  </action>
  <verify>Add a test skill in src/skills/test_skill/SKILL.md and verify it is correctly indexed by SkillRegistry.</verify>
  <done>SkillRegistry automatically discovers and parses metadata for all system skills.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deterministic Command-Dispatch Bypass</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Modify classify_intent node in src/graph/orchestrator.py.
    Check if the user input or classified intent matches a deterministic skill trigger from SkillRegistry.
    If a match is found, route to a new 'command_dispatch' node that bypasses LLM reasoning.
    Execute the skill's execution_command via ClawGuard.
  </action>
  <verify>Run an input that triggers a deterministic skill and verify no LLM call is made to a reasoning node.</verify>
  <done>Sub-millisecond tasks bypass LLM reasoning via direct skill execution.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Progressive Skill Disclosure in Prompts</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Update L1 system prompts in src/graph/orchestrator.py.
    Dynamically inject ONLY the metadata (names/descriptions) of skills relevant to the current classified intent.
    This prevents prompt bloating while keeping L1 aware of relevant capabilities.
  </action>
  <verify>Inspect the generated system prompt for different intents and verify only relevant skills are listed.</verify>
  <done>Orchestrator uses progressive disclosure to keep L1 prompts focused and efficient.</done>
</task>

</tasks>

<verification>
Verify that adding a new skill directory with a SKILL.md file automatically makes the skill available to L1 and that it can be triggered deterministically if configured.
</verification>

<success_criteria>
1. Skills are automatically discovered from filesystem metadata.
2. Deterministic bypass works for matched intents.
3. System prompt stays compact via progressive disclosure.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-orchestration-l1/01-03-SUMMARY.md`
</output>
