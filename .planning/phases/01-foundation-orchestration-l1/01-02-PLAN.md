---
phase: 01-foundation-orchestration-l1
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/tools/verification_wrapper.py, src/core/budget_manager.py, src/graph/orchestrator.py]
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Shell commands are restricted by ClawGuard to authorized directories and commands"
    - "Token usage is tracked and swarm activity halts if budget is exceeded"
    - "Budget ceilings are configurable and enforced at the L1 layer"
  artifacts:
    - path: "src/tools/verification_wrapper.py"
      provides: "ClawGuard shell sandboxing"
    - path: "src/core/budget_manager.py"
      provides: "Token spend tracking and enforcement"
  key_links:
    - from: "src/graph/orchestrator.py"
      to: "src/core/budget_manager.py"
      via: "Budget check on state transitions"
---

<objective>
Implement "ClawGuard" sandboxing and token budgeting to ensure swarm safety and cost control. This establishes the safety layer for agent actions and resource consumption.

Purpose: Prevent unauthorized system access and protect against runaway costs.
Output: Secure shell execution wrapper and token budget manager.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
@/home/ollie/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/graph/orchestrator.py
@src/tools/verification_wrapper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ClawGuard shell sandboxing</name>
  <files>src/tools/verification_wrapper.py</files>
  <action>
    Add a 'ClawGuard' class to src/tools/verification_wrapper.py.
    Implement a safe_exec(command) method that validates the command against a whitelist.
    Enforce directory restrictions (only allow access to specific project subdirectories).
    Use subprocess.run with timeout and restricted environment variables.
  </action>
  <verify>Run test commands: 'ls' (allowed) and 'rm -rf /' (denied) through ClawGuard.</verify>
  <done>ClawGuard correctly filters and executes authorized shell commands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Token Budget Manager</name>
  <files>src/core/budget_manager.py</files>
  <action>
    Create src/core/budget_manager.py with a BudgetManager class.
    Implement methods to track token usage (input/output) and total spend in USD.
    Implement a check_budget() method that raises a SafetyShutdown exception if limits are reached.
    Support configuration of daily and session-based ceilings via config/swarm_config.yaml.
  </action>
  <verify>Simulate high token usage and verify that BudgetManager triggers a shutdown signal.</verify>
  <done>BudgetManager enforces programmatic token spend ceilings.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Security into Orchestrator</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Update src/graph/orchestrator.py to use ClawGuard for all shell-based tool calls.
    Add a budget check at the beginning of the L1 loop or major state transitions.
    Ensure 'total_tokens' is persisted in SwarmState and updated via BudgetManager.
  </action>
  <verify>Run the orchestrator and verify it correctly blocks an unauthorized shell call or shuts down on budget exhaustion.</verify>
  <done>Orchestrator enforces safety guardrails and budget constraints.</done>
</task>

</tasks>

<verification>
Perform a security audit test: verify an agent cannot execute unauthorized shell commands and that a simulated budget overrun halts the swarm.
</verification>

<success_criteria>
1. Shell commands are restricted and validated.
2. Token spend is tracked and capped.
3. Swarm automatically shuts down on security or budget violations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-orchestration-l1/01-02-SUMMARY.md`
</output>
