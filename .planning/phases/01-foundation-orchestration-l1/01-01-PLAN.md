---
phase: 01-foundation-orchestration-l1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/graph/orchestrator.py, src/core/blackboard.py]
autonomous: true
requirements: [ORCH-01, ORCH-02]

must_haves:
  truths:
    - "L1 orchestrator uses LangGraph checkpointer for state suspension"
    - "Agents can read and write to the shared Blackboard on the filesystem"
    - "Blackboard operations are atomic and safe from race conditions"
  artifacts:
    - path: "src/core/blackboard.py"
      provides: "Filesystem-based state management"
    - path: "src/graph/orchestrator.py"
      provides: "L1 orchestration logic"
  key_links:
    - from: "src/graph/orchestrator.py"
      to: "src/core/blackboard.py"
      via: "State updates"
---

<objective>
Consolidate the L1 orchestrator and implement the filesystem-based Blackboard for inter-agent communication. This establishes the core framework for agent delegation and state persistence.

Purpose: Minimize token context usage via suspension and enable robust multi-agent coordination.
Output: Working L1 orchestrator with Blackboard integration.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
@/home/ollie/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-orchestration-l1/01-CONTEXT.md
@src/graph/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate L1 Orchestrator & Persistence</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Refactor src/graph/orchestrator.py to ensure it strictly follows the Phase 1 L1 requirements. 
    Ensure LangGraph's MemorySaver is correctly configured for session-based suspension.
    Remove or stub out L2/L3 nodes that are not yet planned if they interfere with clean L1 operation, 
    but keep the overall structure if it serves as a valid skeleton.
    Add 'blackboard' to the SwarmState definition.
  </action>
  <verify>Run a smoke test script to invoke the orchestrator and verify it saves state to the checkpointer.</verify>
  <done>Orchestrator successfully suspends and resumes state using thread_id.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Blackboard (Filesystem-as-Context)</name>
  <files>src/core/blackboard.py</files>
  <action>
    Create src/core/blackboard.py with a Blackboard class.
    Implement methods: write_state(session_id, key, value), read_state(session_id, key), and list_keys(session_id).
    Use data/inter_agent_comms/ as the base directory.
    Implement file locking using portalocker or a similar mechanism to ensure atomicity.
    State should be stored as JSON or YAML files.
  </action>
  <verify>Create a test script that writes to and reads from the blackboard concurrently to test locking.</verify>
  <done>Blackboard provides atomic read/write access to shared filesystem state.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Blackboard into Orchestrator</name>
  <files>src/graph/orchestrator.py</files>
  <action>
    Update src/graph/orchestrator.py to initialize the Blackboard during the 'classify_intent' or a new 'init' node.
    Ensure every L1 delegation writes the objective to the blackboard for the child agent to pick up.
    Update SwarmState to include a reference or path to the blackboard session.
  </action>
  <verify>Run the orchestrator and check data/inter_agent_comms/ for the expected state files.</verify>
  <done>Orchestrator state and Blackboard state are synchronized during agent delegation.</done>
</task>

</tasks>

<verification>
Verify that L1 can initiate a task, suspend, and that the delegated task details are visible in the data/inter_agent_comms/ directory.
</verification>

<success_criteria>
1. Orchestrator supports thread-based suspension/resume.
2. Inter-agent communication is persisted on the filesystem.
3. Smoke test shows successful delegation via Blackboard.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-orchestration-l1/01-01-SUMMARY.md`
</output>
