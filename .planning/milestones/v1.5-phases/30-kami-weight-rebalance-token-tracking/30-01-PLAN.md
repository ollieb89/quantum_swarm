---
phase: 30-kami-weight-rebalance-token-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/kami.py
  - config/swarm_config.yaml
  - tests/test_kami.py
autonomous: true
requirements:
  - KAMI-06
  - KAMI-07

must_haves:
  truths:
    - "KAMI composite weights sum to 1.0 with alpha=0.08 and delta=0.32"
    - "Existing individual dimension scores are unchanged after weight rebalance"
    - "Composite merit shifts toward fidelity-weighted equilibrium over subsequent cycles"
  artifacts:
    - path: "src/core/kami.py"
      provides: "Rebalanced DEFAULT_WEIGHTS constant"
      contains: '"alpha": 0.08'
    - path: "config/swarm_config.yaml"
      provides: "Matching YAML config weights"
      contains: "alpha: 0.08"
    - path: "tests/test_kami.py"
      provides: "Tests for rebalanced weights and EMA absorption"
  key_links:
    - from: "src/core/kami.py"
      to: "config/swarm_config.yaml"
      via: "matching weight constants"
      pattern: "alpha.*0\\.08"
---

<objective>
Rebalance KAMI DEFAULT_WEIGHTS so Accuracy drops from 30% to 8% and Fidelity rises from 10% to 32%, eliminating the 30% inert weight problem (Accuracy frozen at 0.5). Recovery and Consensus remain unchanged at 35% and 25%.

Purpose: Accuracy has been frozen at 0.5 since there is no thesis_records system yet, making 30% of the composite inert. Shifting that weight to Fidelity (now fed by continuous PersonaScore from Phase 29) makes the merit system responsive to actual agent behavior.

Output: Updated weight constants in code and config, with tests proving correctness and EMA absorption.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-kami-weight-rebalance-token-tracking/30-CONTEXT.md
@.planning/phases/30-kami-weight-rebalance-token-tracking/30-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/core/kami.py:
```python
DEFAULT_WEIGHTS: Dict[str, float] = {
    "alpha": 0.30,  # Accuracy weight
    "beta": 0.35,   # Recovery weight
    "gamma": 0.25,  # Consensus weight
    "delta": 0.10,  # Fidelity weight
}

def compute_merit(dims: KAMIDimensions, weights: Dict[str, float]) -> float:
    """alpha*accuracy + beta*recovery + gamma*consensus + delta*fidelity, clamped [0.1, 1.0]"""

def apply_ema(prev: float, signal: float, lam: float) -> float:
    """lam * signal + (1.0 - lam) * prev"""
```

From config/swarm_config.yaml (lines 248-258):
```yaml
kami:
  alpha: 0.30
  beta: 0.35
  gamma: 0.25
  delta: 0.10
  lambda: 0.9
  default_merit: 0.5
  merit_floor: 0.1
  merit_ceil: 1.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add weight rebalance tests</name>
  <files>tests/test_kami.py</files>
  <behavior>
    - test_rebalanced_weights: DEFAULT_WEIGHTS["alpha"] == 0.08, DEFAULT_WEIGHTS["delta"] == 0.32, sum == 1.0
    - test_weight_rebalance_shifts_composite: Given identical KAMIDimensions(accuracy=0.5, recovery=0.8, consensus=0.7, fidelity=0.9), compute_merit with new weights yields higher composite than with old weights (because fidelity=0.9 now has 32% weight instead of 10%)
    - test_ema_absorption_preserves_dimensions: Calling apply_ema on each dimension individually produces same result regardless of composite weights (dimensions are independent of weights)
  </behavior>
  <action>
Add three new test functions to `tests/test_kami.py`:

1. `test_rebalanced_weights` in `TestConstants` class — assert alpha=0.08, delta=0.32, beta=0.35, gamma=0.25, and sum(values)==1.0.

2. `test_weight_rebalance_shifts_composite` in `TestComputeMerit` class — construct KAMIDimensions with accuracy=0.5, recovery=0.8, consensus=0.7, fidelity=0.9. Compute merit with DEFAULT_WEIGHTS. Assert result > 0.7 (fidelity-dominant composite). Also verify it differs from the old-weights result by computing with {"alpha":0.30,"beta":0.35,"gamma":0.25,"delta":0.10}.

3. `test_ema_absorption_preserves_dimensions` — verify that apply_ema(0.5, 0.9, 0.9) returns same value regardless of which dimension it represents. This confirms EMA operates on individual dimension scores, not composites (KAMI-07).

Run tests first — they MUST fail (RED phase).
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_kami.py::TestConstants::test_rebalanced_weights tests/test_kami.py::TestComputeMerit::test_weight_rebalance_shifts_composite -x -q</automated>
  </verify>
  <done>Three new tests exist and fail with current weights (alpha=0.30, delta=0.10)</done>
</task>

<task type="auto">
  <name>Task 2: Rebalance weights in code and config</name>
  <files>src/core/kami.py, config/swarm_config.yaml</files>
  <action>
1. In `src/core/kami.py` line 28-33, update DEFAULT_WEIGHTS:
   - `"alpha": 0.08` (was 0.30) with comment `# Accuracy weight (reduced -- frozen at 0.5 until thesis_records)`
   - `"beta": 0.35` (unchanged)
   - `"gamma": 0.25` (unchanged)
   - `"delta": 0.32` (was 0.10) with comment `# Fidelity weight (increased -- fed by continuous PersonaScore)`

2. In `config/swarm_config.yaml` lines 250-253, update matching values:
   - `alpha: 0.08`
   - `beta: 0.35` (unchanged)
   - `gamma: 0.25` (unchanged)
   - `delta: 0.32`

CRITICAL: Both locations must match. The existing `test_default_weights_sum_to_one` test validates the code constant sums to 1.0.

No migration script needed — EMA naturally absorbs the weight change. Individual dimension scores (accuracy, recovery, consensus, fidelity) are unchanged. Only the composite formula weighting shifts (KAMI-07).
  </action>
  <verify>
    <automated>.venv/bin/python3.12 -m pytest tests/test_kami.py -x -q</automated>
  </verify>
  <done>DEFAULT_WEIGHTS shows alpha=0.08 and delta=0.32 in both code and config. All KAMI tests pass including new rebalance tests and existing sum-to-one test.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.12 -m pytest tests/test_kami.py -x -q` — all KAMI tests green
- `grep "alpha" src/core/kami.py config/swarm_config.yaml` — both show 0.08
- `grep "delta" src/core/kami.py config/swarm_config.yaml` — both show 0.32
</verification>

<success_criteria>
- KAMI DEFAULT_WEIGHTS alpha=0.08, delta=0.32, sum=1.0
- config/swarm_config.yaml matches code constants
- Existing KAMI tests continue to pass (no score reset, no dimension changes)
- New tests verify specific weight values and composite shift
</success_criteria>

<output>
After completion, create `.planning/phases/30-kami-weight-rebalance-token-tracking/30-01-SUMMARY.md`
</output>
